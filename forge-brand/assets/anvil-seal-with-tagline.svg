<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 240 48" fill="none" role="img" aria-labelledby="forge-tagline">
  <title id="forge-tagline">Forge-built — designed to endure</title>

  <!-- Anvil (scaled proportionally, centered vertically) -->
  <g transform="translate(8, 8)">
    <path d="M6 24h20v3H6z" fill="currentColor"/>
    <path d="M8 16h16v8H8z" fill="currentColor"/>
    <path d="M8 12h4v4H8z" fill="currentColor"/>
    <path d="M10 8h12l2 4H8z" fill="currentColor"/>
    <path d="M20 12h4v4h-4z" fill="currentColor"/>
  </g>

  <!-- Tagline text converted to paths -->
  <!-- NOTE: Replace this placeholder with actual path-converted text from design tool -->
  <!-- For now, keeping semantic text with instruction to convert before ship -->
  <text x="52" y="28"
        font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
        font-size="12"
        font-weight="500"
        fill="currentColor"
        opacity="0.85">
    Forge-built — designed to endure
  </text>

  <!-- PRODUCTION TODO: Convert text to paths using:
       - Figma: Select text → Right-click → "Outline Stroke"
       - Illustrator: Select text → Type → Create Outlines
       - Inkscape: Select text → Path → Object to Path
  -->
</svg>
